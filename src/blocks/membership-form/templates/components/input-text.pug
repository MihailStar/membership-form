mixin input-text({ id, modifiers = [] })

  -
    const b = 'membership-form';
    const e = 'input-text';
    const classes = [`${b}__${e}`].concat(modifiers.map((m) => `${b}__${e}_${m}`));

  input(class=classes id=id type='text')&attributes(attributes)
