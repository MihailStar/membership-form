mixin label-for-input-text({ title, id, modifiers = [] })

  -
    const b = 'membership-form';
    const e = 'label-for-input-text';
    const classes = [`${b}__${e}`].concat(modifiers.map((m) => `${b}__${e}_${m}`));

  label(class=classes for=id)&attributes(attributes)= title
